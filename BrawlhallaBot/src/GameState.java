// Define enum for different game states
public enum GameState {
    MAIN_MENU,
    //210x20
    Game_Select,
    Room_Type,
    LOBBY,
    GAME_RULES,
    LOBBY_RULES,
    FULL_LOBBY,
    IN_GAME,
    IN_GAME_MENU,
    OUT_Game,
    POST_Game,
    MATCH_REWARDS,
    UNKNOWN,
    // Add more game states as needed
}
